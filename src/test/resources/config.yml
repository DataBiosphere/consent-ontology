server:
  adminConnectors:
    - type: http
      port: 8181
  applicationConnectors:
    - type: http
      port: 8180
logging:
  level: INFO
    # logger specific levels
  loggers:
    "org.semanticweb": ERROR
elasticSearch:
  servers: ["127.0.0.1:9300"]
  clusterName: "elasticsearch_ci"
  index: ontology
googleStore:
  password:
  endpoint:
  bucket:
storeOntology:
  bucketSubdirectory:
  configurationFileName:

